<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LokalAssignment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/primary_yellow</item>
    </style>

    <style name="Theme.LokalAssignment" parent="Base.Theme.LokalAssignment" />
</resources>